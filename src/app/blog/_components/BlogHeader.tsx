import { SectionPageHeader } from "@/components/layout/page-header/SectionPageHeader"

export function BlogHeader() {
    return (
        <SectionPageHeader
            title="Blog"
            description="Thoughts on characters, stories, films, the reason behind my artworks, and more."
            currentLabel="Blog"
        />
    )
} 

